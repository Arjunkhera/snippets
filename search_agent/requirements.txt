# Core dependencies for Search Agent

# LangGraph and LangChain
langgraph>=0.0.20
langchain>=0.1.0
langchain-core>=0.1.0

# LLM Provider
anthropic>=0.18.0

# Data validation and parsing
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Configuration
python-dotenv>=1.0.0

# Elasticsearch (for validation, not client in Phase 1)
elasticsearch-dsl>=8.0.0

# Utilities
typing-extensions>=4.5.0

# Testing (development only)
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
